module.exports = {
    extends: ['next', 'next/core-web-vitals'],
    rules: {
      'react/no-unescaped-entities': 'off', // Disable this rule if needed
      '@next/next/no-img-element': 'off',  // Disable this rule if needed
    },
  };
  